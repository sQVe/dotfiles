---
description: Route any input (task/note/reference/URL) to the correct weekly file section
---

Use the capture skill exactly as written.

Read `/home/sqve/.dotfiles/claude/skills/capture/SKILL.md` and follow its process to capture the input `$ARGUMENTS` into the current weekly file.

If `$ARGUMENTS` is empty, ask the user: "What do you want to capture?"
