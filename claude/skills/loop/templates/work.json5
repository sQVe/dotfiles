{
  // DATA ONLY. Process with @LOOP.md
  // Verify: task > ticket > config (closest non-null wins per property)
  "$schema": "/home/sqve/.claude/skills/loop/schema.json",
  "config": {
    "stuckThreshold": 3,
    "verify": {
      "lint": "pnpm run lint",
      "typecheck": "pnpm run typecheck",
      "test": "pnpm run test:ci",
    },
  },
  "guidance": {
    "docs": ["CLAUDE.md"],      // Style guides, conventions
    "files": [],                // Directories to work in
    "reference": [],            // Files to copy patterns from
    "notes": "",
  },
  // Optional: group tickets into PRs (run /pr after last ticket in group completes)
  // "prs": [
  //   { "title": "feat: add validation", "tickets": ["add input validation"] },
  //   { "title": "chore: upgrade dependencies", "tickets": ["upgrade library with breaking changes"] },
  // ],
  "tickets": [
    // SIMPLE: inherits all config.verify, no overrides needed
    {
      "name": "add input validation",
      "type": "feat",
      "tasks": [
        { "description": "Add email format validation to signup form", "done": false },
        { "description": "Add password strength indicator", "done": false },
      ],
    },

    // COMPLEX: ticket disables test, task re-enables + adds manual
    {
      "name": "upgrade library with breaking changes",
      "type": "chore",
      "verify": {
        "test": null,  // Tests fail mid-migration, disable at ticket level
      },
      "tasks": [
        {
          "description": "Update package.json dependencies",
          "done": false,
        },
        {
          "description": "Fix breaking API changes",
          "done": false,
          "steps": [
            "Update deprecated method calls",
            "Fix changed type signatures",
            "Remove deleted exports",
          ],
        },
        {
          "description": "Verify migration complete",
          "done": false,
          "verify": {
            "test": "pnpm run test:ci",  // Re-enable for final task
            "manual": ["No console warnings", "Feature works in browser"],
          },
        },
      ],
    },
  ],
}
